{
    "buildCommand": "prisma generate && next build --webpack",
    "outputDirectory": ".next",
    "installCommand": "npm install",
    "framework": "nextjs",
    "regions": [
        "cdg1"
    ]
}